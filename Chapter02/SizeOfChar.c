//
// Created by Junghun Lee on 12/26/23.
//
/*
#include <stdio.h>

int main(void) {
    char ch  = 'A'; // 문자형(character) 상수

    printf("sizeof('A') : %d\n", sizeof('A')); // 4 byte
    printf("sizeof(ch) : %d\n", sizeof(ch)); // 1 byte
    // 상수는 4byte로 사용, 변수의 경우 1byte로 저장한다. 메모리 관리 효율을 올리기 위해서 지정한 듯 하다

    return 0;
}
 */